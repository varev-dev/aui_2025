spring:
  application:
    name: gateway-service
  cloud:
    config:
      #enabled: false 
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      fail-fast: true
      retry:
        max-attempts: 20
        initial-interval: 1000
        max-interval: 2000
