# AUI 2025

## How to run
```bash
cd cluster-service && mvn spring-boot:run   # :8081
cd node-service && mvn spring-boot:run      # :8082
cd gateway-service && mvn spring-boot:run   # :8080
cd hpc-frontend && ng serve                 # :4200
```